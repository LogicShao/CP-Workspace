#include <bits/stdc++.h>

#define multi_test 0

using LL = long long;
using PII = std::pair<int, int>;

void solve() {}

int main() {
  std::cin.tie(nullptr)->sync_with_stdio(false);

  int T = 1;
  if constexpr (multi_test) {
    std::cin >> T;
  }

  while (T--) {
    solve();
  }
}